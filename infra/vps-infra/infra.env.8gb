# Profil resource VPS-INFRA — TUMBUH (VPS 8GB, pilot penuh hingga ~15 tenant).
# Pakai: docker compose --env-file infra.env.8gb up -d --build
# DEPRECATED: shared gateway runtime is owned by lumite-gateway. Do not use for production.
# Historical capacity note: ~15 WhatsApp sessions on an 8GB profile.
GW_MEM_LIMIT=6g
GW_SHM=1gb
MQTT_MEM_LIMIT=256m
BRIDGE_MEM_LIMIT=256m
